---
title: "Get Power Status"
api: "GET /v2/instance/{instance_id}/power"
description: "Retrieve the current power status of an instance"
---

## Overview

This endpoint returns the current power status of a specific instance. Use this to check whether an instance is powered on, off, or in a transitional state.

## Path Parameters

<ParamField path="instance_id" type="string" required>
  The unique identifier of the instance to query.
</ParamField>

## Response

<ResponseField name="message" type="string">
  The current power status of the instance (e.g., "on", "off").
</ResponseField>

<ResponseField name="status" type="boolean">
  Indicates whether the request was successful.
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET "https://api.dallas-idc.com/v2/instance/{instance_id}/power" \
  -H "Authorization: Bearer YOUR_API_TOKEN"
```

```python Python
import requests

response = requests.get(
    "https://api.dallas-idc.com/v2/instance/{instance_id}/power",
    headers={"Authorization": "Bearer YOUR_API_TOKEN"}
)

print(response.json())
```

```javascript JavaScript
const response = await fetch(
  "https://api.dallas-idc.com/v2/instance/{instance_id}/power",
  {
    headers: {
      "Authorization": "Bearer YOUR_API_TOKEN"
    }
  }
);

const data = await response.json();
console.log(data);
```
</RequestExample>

<ResponseExample>
```json 200
{
  "message": "on",
  "status": true
}
```

```json 404
{
  "message": "Instance not found",
  "status": false
}
```
</ResponseExample>
