---
title: "Get Instance Subnets"
openapi: "GET /v2/instance/{instance_id}/subnet"
---

Retrieve the IP subnets assigned to a specific instance.

## Path Parameters

<ParamField path="instance_id" type="string" required>
  The unique identifier (UUID) of the instance to query.
</ParamField>

## Response

Returns an array of [IpBlock](/api-reference/schemas/ip-block) objects containing:

- Network block with CIDR notation
- Network address and netmask
- Device assignment information
- Network type (public or private)
- Assignment type (direct or floating)

## Example Request

```bash
curl -X GET "https://api.dallas-idc.com/v2/instance/{instance_id}/subnet" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

## Example Response

```json
[
  {
    "block": "192.168.1.0/24",
    "network": "192.168.1.0",
    "cidr": 24,
    "netmask": "255.255.255.0",
    "device": "eth0",
    "created_at": "2024-01-15T10:30:00Z",
    "network_type": "public",
    "assignment_type": "direct"
  },
  {
    "block": "10.0.0.0/24",
    "network": "10.0.0.0",
    "cidr": 24,
    "netmask": "255.255.255.0",
    "device": "eth1",
    "created_at": "2024-01-15T10:30:00Z",
    "network_type": "private",
    "assignment_type": "direct"
  }
]
```

## Use Cases

- **Network Inventory**: List all IP addresses assigned to an instance
- **Firewall Configuration**: Get subnet information for configuring network security rules
- **DNS Management**: Retrieve IP assignments for DNS record updates
- **Capacity Planning**: Review IP allocation across instances

## Related Endpoints

- [Get Instance](/api-reference/instances/get-instance) - Get full instance details including subnets
- [Assign IP Subnet](/api-reference/instances/assign-instance-subnet) - Assign a new IP subnet to an instance
- [Remove IP Subnet](/api-reference/instances/remove-instance-subnet) - Remove an IP subnet from an instance
