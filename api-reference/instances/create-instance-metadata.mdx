---
title: "Create Instance Metadata"
api: "POST /v2/instance/{instance_id}/metadata"
description: "Create a new metadata key-value pair for an instance"
---

## Path Parameters

<ParamField path="instance_id" type="string" required>
  The unique identifier of the instance
</ParamField>

## Body Parameters

<ParamField body="key" type="string" required>
  The key for the metadata entry
</ParamField>

<ParamField body="value" type="string" required>
  The value for the metadata entry
</ParamField>

<ParamField body="mutability" type="integer" default="1">
  The mutability setting for the metadata. Controls whether the metadata can be modified after creation.
</ParamField>

## Response

<ResponseField name="key" type="string">
  The metadata key
</ResponseField>

<ResponseField name="value" type="string">
  The metadata value
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://api.dallas-idc.com/v2/instance/{instance_id}/metadata" \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "key=environment&value=production"
```

```python Python
import requests

instance_id = "your-instance-id"
url = f"https://api.dallas-idc.com/v2/instance/{instance_id}/metadata"

headers = {
    "Authorization": "Bearer YOUR_API_TOKEN"
}

data = {
    "key": "environment",
    "value": "production"
}

response = requests.post(url, headers=headers, data=data)
print(response.json())
```

```javascript JavaScript
const instanceId = "your-instance-id";

fetch(`https://api.dallas-idc.com/v2/instance/${instanceId}/metadata`, {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_TOKEN",
    "Content-Type": "application/x-www-form-urlencoded"
  },
  body: new URLSearchParams({
    key: "environment",
    value: "production"
  })
})
  .then(response => response.json())
  .then(data => console.log(data));
```
</RequestExample>

<ResponseExample>
```json 201
{
  "key": "environment",
  "value": "production"
}
```

```json 400
{
  "message": "Bad request",
  "status": false
}
```

```json 403
{
  "message": "Unauthorized request",
  "status": false
}
```

```json 422
{
  "message": "Invalid parameters",
  "status": false
}
```
</ResponseExample>
