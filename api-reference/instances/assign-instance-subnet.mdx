---
title: "Assign IP Subnet"
openapi: "POST /v2/instance/{instance_id}/subnet"
---

Assign an IP subnet to a specific instance.

## Path Parameters

<ParamField path="instance_id" type="string" required>
  The unique identifier (UUID) of the instance to assign the subnet to.
</ParamField>

<ParamField path="subnet" type="string" required>
  The IP subnet to assign to the instance.
</ParamField>

## Response

Returns a [Job](/api-reference/schemas/job) object that tracks the subnet assignment operation.

## Example Request

```bash
curl -X POST "https://api.dallas-idc.com/v2/instance/{instance_id}/subnet" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "subnet=192.168.1.0/24"
```

## Example Response

```json
{
  "created_at": "2024-01-15T10:30:00Z",
  "update_at": "2024-01-15T10:30:00Z",
  "status": "pending",
  "name": "assign_ip_subnet",
  "job_identifier": "job-abc123",
  "display_name": "Assign IP Subnet",
  "metadata": [],
  "statuses": []
}
```

## Use Cases

- **Network Expansion**: Add additional IP addresses to an instance for hosting multiple services
- **Load Balancing**: Assign floating IPs for high availability configurations
- **Multi-tenant Hosting**: Allocate dedicated IP subnets for different customers or applications

## Related Endpoints

- [Get Instance Subnets](/api-reference/instances/get-instance-subnets) - List all IP subnets assigned to an instance
- [Remove IP Subnet](/api-reference/instances/remove-instance-subnet) - Remove an IP subnet from an instance
- [Get Job](/api-reference/jobs/get-job) - Check the status of the assignment job
